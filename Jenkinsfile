node {
    stage('Checkout') {
        checkout scm
    }
    stage('Check NPM Ver') {
        sh 'echo "Hello World"'
    }
    // stage('Build') {
    //     sh 'npm install'
    // }
    // stage('Test') {
    //     sh './jenkins/scripts/test.sh' 
    // }
}